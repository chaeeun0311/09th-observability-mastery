# Week 1 

## 0. 기본 정보
- 주차: 2주차
- 읽은 범위: Chapter 1. 관측 가능성의 개념과 방향성 (p.1 – 67)
- 선택 유형:
  - [x] 책 범위 정리
  - [ ] 딥다이브 주제 정리

## 1. 이번 주 내용 한 줄 요약

> 메트릭 / 추적 / 로그를 따로 보는 게 아니라 서로 연결해서 봐야, 시스템에서 문제가 생겼을 때 원인을 빠르게 찾을 수 있다는 게 관측 가능성의 핵심이다.

---

## 2. 책 내용 핵심 요약

1. **관측 가능성은 모니터링과 다름** 
   - 모니터링이 "지금 뭔가 이상하다"를 알려준다면, 관측 가능성은 "왜 이상한지, 어디서 문제가 생겼는지"까지 시스템 내부 흐름을 보여줄 수 있음 

2. **세 가지 신호(Signal)가 핵심** 
   - 메트릭(수치), 추적(경로), 로그(상세 내용)의 세 정보가 모여야 장애의 전체 그림이 보임

3. **메트릭 중에서는 히스토그램이 중요** 
   - 응답시간 데이터를 범위(버킷)별로 나눠서 분포를 표현하는 방식
   - le 태그를 이용해서 "0.1초 이하로 처리된 요청이 몇 %냐"를 계산 가능  
→ SLO(서비스 목표 수준) 달성 여부를 판단하는 기준이 됨

1. **세 가지 신호를 연결(상관관계)하는 게 진짜 목표** 
   - 메트릭에서 이상을 감지 -> 추적으로 어느 서비스 문제인지 파악 -> 로그로 정확한 원인 확인 : 이 흐름이 자동으로 연결될 때 관측 가능성의 가치 생성

---

## 3. 중요하다고 생각한 개념

### 개념 1. 모니터링과 관측 가능성의 차이

#### 개념 설명

- 모니터링은 시스템 바깥에서 "지금 CPU가 몇 %다, 응답이 왔다 안 왔다" 정도를 확인하는 것
- 관측 가능성은 한 발 더 들어가서, 요청이 시스템 내부에서 어떤 경로로 흘렀는지, 어디서 느려졌는지까지 볼 수 있게 해주는 것  
=> 핵심 차이는 **내부 흐름을 볼 수 있느냐 없느냐**

#### 중요하다고 생각한 이유
 
- 모니터링만으로는 "이상하다"는 건 알아도 "왜 이상한지"는 모른다는 걸 알고 나서, 왜 관측 가능성이라는 개념이 따로 존재하는지 이해하게 되었음

#### 운영 관점에서의 의미

- 관측 가능성이 갖춰지면 추적(Tracing)으로 어느 서비스에서 병목이 생겼는지 바로 찾아갈 수 있음

---

### 개념 2. 세 가지 신호의 상관관계

#### 개념 설명

- 메트릭, 추적, 로그는 각각 다른 역할 수행
- 메트릭 : 수치로 이상을 감지
- 추적 : 어느 서비스가 문제인지 경로를 추적
- 로그 : 정확히 어떤 에러가 왜 났는지 상세 내용을 확인
  
=> 이 세 가지가 Trace ID 같은 공통 식별자로 연결될 때, 메트릭에서 추적으로, 추적에서 로그로 이동하며 원인을 추적하는 흐름이 가능해짐

#### 중요하다고 생각한 이유
 
- 세 가지를 따로 구축하는 건 기존 모니터링이랑 다를 게 없어서 연결이 핵심이라는 점이 이 챕터의 결론이라고 판단

#### 운영 관점에서의 의미

-  장애 대응 시간 감소
-  메트릭에서 이상 감지 -> 추적으로 원인 서비스 파악 -> 로그로 정확한 에러 확인, 이 흐름이 자동화되면 사람이 직접 로그를 뒤지는 시간이 없어짐

---

## 4. 이해가 어려웠던 부분

### 어려웠던 부분
- Chapter 1에 나오는 개념들이 대부분 처음 접하는 것들이라, 어느 부분이 어려웠는지 특정하기보다 전반적으로 생소하게 느껴졌다. 읽으면서 개념의 흐름은 어느 정도 따라갔지만, 각 개념이 실제로 어떻게 쓰이는지는 아직 감이 오지 않는다.

## 5. 추가로 찾아본 내용
 
### 추가로 찾아본 키워드
 
없음
 
### 찾아본 내용 요약
 
없음
 
### 책 내용과 연결되는 부분
 
없음

## 6. 내 생각, 경험, 질문

> **질문 1.** `le` 값(버킷 경계)은 처음에 어떻게 설정하는지 - 책에서는 `le="0.1"`, `le="0.25"`, `le="1.0"` 같은 값이 나오는데, 이게 임의로 정한 건지 서비스 특성에 따라 달라지는 건지 — 실제 설계할 때 기준이 있는지 궁금합니다

> **질문 2.** 책은 프로메테우스/그라파나/예거를 기준으로 설명하는데, 실무에서는 메트릭 + 추적 + 로그를 위해 어떤 도구를 쓰는지, 그리고 그 도구를 선택한 이유가 무엇인지 궁금합니다

## 7. 발표용 요약
 
### 발표 흐름
 
1. 이번 주 책 범위 소개
2. 핵심 내용 3가지 요약
3. 중요하다고 생각한 개념 1~2개 설명
4. 이해가 어려웠던 부분 또는 추가로 찾아본 내용 공유
5. 내 생각, 경험, 질문 중 하나를 골라 공유
6. 같이 이야기해보고 싶은 지점 제시


### 발표 핵심 메시지
 
> 메트릭/추적/로그는 따로 보면 파편적이다. 이 셋이 연결될 때 비로소 장애의 원인을 빠르게 찾을 수 있다 — 그게 Chapter 1이 말하는 관측 가능성의 목표다.
 
---
 
## 8. 참고 자료